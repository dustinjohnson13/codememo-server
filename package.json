{
  "name": "codememo-server",
  "version": "1.0.0",
  "description": "Backend processing for CodeMemo",
  "main": "handler.js",
  "scripts": {
    "test": "jest",
    "integration-test": "jest --testMatch='**/integrationTest/**/*integrationTest.js'",
    "all-test": "jest --testMatch='**/*test.js' --testMatch='**/*integrationTest.js'",
    "test-watch": "jest --watch",
    "all-test-watch": "jest --watch --testMatch='**/*test.js' --testMatch='**/*integrationTest.js'"
  },
  "babel": {
    "presets": [
      "es2015",
      "flow"
    ]
  },
  "author": "Dustin Johnson",
  "license": "ISC",
  "devDependencies": {
    "babel-jest": "^20.0.3",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-flow": "^6.23.0",
    "dynamodb-local": "^0.0.18",
    "flow-bin": "^0.52.0",
    "flow-typed": "^2.1.5",
    "jest": "^20.0.4"
  },
  "dependencies": {
    "aws-sdk": "^2.99.0",
    "freeport": "^1.0.5",
    "uuid": "^3.1.0"
  }
}
